#include <stdio.h>
#include <stdlib.h>

void main()
{
	/*float fInput1 = 0, fInput2 = 0;
	printf("Input float data: ");
	
	scanf("%f %f", &fInput1, &fInput2);
	printf("%.2f == %.2f + %.2f\n", fInput1 + fInput2, fInput1, fInput2);

	char szBuffer[128] = {0};
	printf("Input string: ");

	scanf("%s", &szBuffer[57]);
	puts(&szBuffer[57]);

	char onechar[3];
	
	onechar[0] = getch();
	onechar[1] = getch();
	onechar[2] = getch();
	printf("%c", onechar[1]);
	
	char szbuffer[33];
	gets(szbuffer);
	printf("%d %s",szbuffer, szbuffer);
	
	char onechar;
	onechar = getch();
	printf("%d", onechar);
	
	int one;
	
	printf("%o, %x", one, one);
	
	int a;
	int b, c;
	double da;
	char ch;
	
	a = 10;
	b = a;
	c = a +20;
	da = 3.6;
	ch = 'A';
	
	printf("%d\n", a);
	printf("%d\n", b);
	printf("%d\n", c);
	printf("%.1lf\n", da);
	printf("%c\n", ch);
	
	return 0; 
	
	char A1 = 'a';
	char A2 = 65;
	
	printf("i want %c of goint : %c\n", A1, A1);
	printf("it is %d code : %d\n", A2, A2);
	
	return 0; 
	
	int income = 0; ---> =0을 치므로써 선언된 변수의 값을 0으로 모두 초기화 시기키는 것. 초기화를 안하면 쓰레기 값들로 채워져있다. 
	double tax;
	
	income = 456;
	const double tax_rate = 0.12; ---> const는 변수 선언과 동시에 초기화를 시켜야한다. 이때 초기화 된 값은 fix된다. 
	
	tax = income * tax_rate;
	printf("the price of your tax : it is %.2lf.", tax); 
	
	int kor, eng, mat = 0;
	int tot = 0;
	
	kor = 70;
	eng = 80;
	mat = 90;
	tot = kor + eng + mat;
	printf("rnrdj : %d ", kor);
	printf("duddu : %d ", eng);
	printf("tngkr : %d\n", mat);
	printf("chdwja : %d", tot); 
	
	int a;
	
	scanf("%d",&a);
	printf("입력한 값 : %d", a); --->scanf는 입력전까지 출력되지 않는다.  
	
	int a = 10, b = 20;
	int res;

	
	res = a>b;
	printf("a>b : %d\n", res); 
	
	int a = 30;
	int res;
	
	res = (a>10)&&(a<20);
	printf("(a>10)&&(a<20) : %d\n", res);
	
	res = (a>10)||(a<20);
	printf("(a>10)||(a<20) : %d\n", res);
	
	double a = 4.0, b = 1.2;
	
	printf("a + b = %.1lf\n",a, b, a + b);
	printf("a - b = %.1lf\n",a ,b, a-b);
	printf("a * b = %.1lf\n",a, b, a*b);
	printf("a / b = %.1lf\n",a ,b, a/b);
	
	int a,b, tot;
	double avg;
	
	printf("averge of two objects : ");
	scanf("%d, %d",&a,&b);
	tot = a + b;
	avg = tot / 2.0;
	
	printf("averge : %.1f\n", avg); 
	
	
	int res;
	int kor, eng, mat = 0;
	int credits;
	kor = 3;
	eng = 5;
	mat  =4;
	credits = kor + eng + mat;
	
	
	double kscore, escore, mscore = 0;
	kscore = 3.8;
	escore = 4.4;
	mscore = 3.9;
	double grade = (kscore + escore + mscore)/3.0;
	
	
	res = (credits >=10)&&(grade >4.0);
	printf("%d", res);
	*/
	
	printf("be happy");
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	 
	
	 
	
	
	
	
	

		
}
